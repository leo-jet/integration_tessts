# Environment variables for integration tests

# ============================================
# API Configuration
# ============================================
API_BASE_URL=https://your-api-endpoint.com
API_TIMEOUT=30

# ============================================
# Azure OAuth2 Configuration
# ============================================
# AZURE_TENANT_ID is optional if specified in each app's oauth_config
# Use this for a global tenant_id shared by all apps
AZURE_TENANT_ID=00000000-0000-0000-0000-000000000000

# Application Credentials (referenced by apps.json)
APP_CLIENT_ID=00000000-0000-0000-0000-000000000000
APP_CLIENT_SECRET=your_client_secret_here

# User App Credentials (for role_priority="user")
USER_APP_CLIENT_ID=00000000-0000-0000-0000-000000000000

# MSAL User Token (for role_priority="user" apps)
# Generate this token using MSAL interactive flow before running tests
# Or use MOCK_USER_TOKEN for testing without real authentication
USER_APP_MSAL_TOKEN=your_msal_token_here
MOCK_USER_TOKEN=mock_user_token_for_testing

# APIM Scope for MSAL authentication
APIM_SCOPE=https://management.azure.com/.default

# ============================================
# Retry Configuration (Optional)
# ============================================
RETRY_MAX_ATTEMPTS=3
RETRY_BACKOFF_FACTOR=2.0
RETRY_INITIAL_DELAY=1.0
